import React from 'react';

// Ported from includes/footer.php (closing </main> markup only — the
// <script> tags it loaded are replaced by React/Vite's own bundling).
export default function Footer() {
  return null;
}
